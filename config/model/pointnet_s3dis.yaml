# training parameters
train:
  model: pointnet_sem_seg # model name
  batch_size: 20 # batch size during training
  epochs: 128 # epoch to run
  lr: 0.001 # initial learning rate

# testing parameters
test:
  batch_size: 3
  shuffle: False
  num_workers: 4

  # data parameters
data:
  name: "s3dis" # data folder
  debug_mode: True # if true, small data is used